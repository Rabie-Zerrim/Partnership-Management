export interface Partnership {
    idPartnership?: number;
    partnershipStatus: string;
    entreprise?: any; // Replace with proper Entreprise model if available
    proposals?: any;  // Replace with proper Proposal model if available
  }
  