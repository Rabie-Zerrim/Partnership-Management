/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { PartnershipEditComponent } from './partnership-edit.component';

describe('PartnershipEditComponent', () => {
  let component: PartnershipEditComponent;
  let fixture: ComponentFixture<PartnershipEditComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ PartnershipEditComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(PartnershipEditComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
